# Stage3 需求池与执行清单（可持续补充）

> 目的：承接 Stage2 代码核对后的未完成项，并作为 Stage3 新需求与细节的统一落盘入口。
>
> 更新规则：
> - 你后续补充的新需求，直接追加到「3. 新增需求池（待补充）」。
> - 每条需求都要绑定验收标准（DoD）与影响面。
> - 设计、测试、发布文档引用本文件编号，避免需求漂移。

## 1. 来源与基线

- 基线代码：`main`（核对日期：2026-03-01）
- 基线文档：`.docs/stage2/*`（冻结口径）
- 迁移目标：将 Stage2 未落地项与 Stage3 新增需求统一纳入执行

## 2. Stage2 未落地迁移项（已确认）

### 2.1 S3-RQ-001 电力模型补齐

- 状态：`todo`
- 来源：Stage2 冻结外移项 + 代码核对
- 需求：
  - 全设备显式补齐 `powerDemand`
  - 未使用字段显式填 `0`（不得省略）
  - 提供总耗电/总发电/净功率
  - 提供即时值、10 分钟、1 小时统计（标准秒）
  - 设备属性页展示功率字段（发电设备 `powerDemand=0`）
- 代码现状证据：
  - 当前仅 `requiresPower`：`src/domain/types.ts`、`src/domain/registry.ts`
  - 仍有 `NO_POWER` 停机：`src/sim/engine.ts`
- DoD：
  - 设备功率字段全量可读且缺失率为 0
  - 统计窗口按标准秒滚动
  - UI 可观测且与仿真口径一致
  - 发电设备 `powerDemand=0` 展示正确

### 2.2 S3-RQ-002 缺电语义改造

- 状态：`todo`
- 需求：
  - 运行形态仅有“运转 / 停机”，无降速语义
  - 当前缺电为 `NO_POWER` 阻塞停机，Stage3 改造为双状态阻塞
  - `NO_POWER` 拆分为：
    - `LOW_POWER`：系统用电量超过发电量且全局电池已耗尽（阻塞）
    - `OUT_OF_POWER_RANGE`：设备不在供电范围内（阻塞）
- 代码现状证据：
  - `NO_POWER` 位于硬阻塞集合：`src/sim/engine.ts`
- DoD：
  - 缺电时设备阻塞停机（无降速形态）
  - `LOW_POWER` 与 `OUT_OF_POWER_RANGE` 触发条件准确

### 2.2.1 电力字段补充口径（2026-03-03）

- `requiresPower=false` 且 `powerDemand>0` 是合法组合。
- 语义：设备存在能耗统计，但不纳入“供电覆盖不足导致阻塞”判定。
- 设备供电能力字段（`powerSupply`）已从当前阶段设备定义移除，后续单独设计。

### 2.3 S3-RQ-007 反应池口径确认

- 状态：`done`
- 决策：反应池已完成验收，正确口径为“5 共享槽位”。
- 备注：该项不再进入 Stage3 开发，仅作为基线回归检查。

### 2.4 S3-RQ-011 全局电池模型定义

- 状态：`todo`
- 来源：Stage3 契约补齐项
- 需求：
  - 在 Stage3 中定义全局电池模型字段与数据源（非设备字段）。
  - 明确 `LOW_POWER` 判定所依赖的全局电池状态口径。
- DoD：
  - 全局电池字段命名、默认值与更新时机在契约文档固化。
  - `LOW_POWER` 判定链路可回归验证。

## 3. 新增需求池（待补充）

> 你后续补充 Stage3 新需求时，按以下模板追加：

### 3.1 S3-RQ-008 产线规划器流程图视图与视角切换

- 状态：`todo`
- 迭代范围备注（2026-03-03）：当前迭代先交付 `byBuilding`（流程图-按设备）Tab；`byProduct` 视角保留在后续迭代，不影响需求编号与最终目标。
- 业务价值：
  - 当前树状列表在跨链路理解上可读性不足，流程图可提升依赖关系与瓶颈定位效率。
  - 提供“按产物 / 按设备”两种观察视角，支持不同使用场景下的快速分析。
- 详细需求：
  - 在产线规划器中新增流程图展示，不替代现有树状列表。
  - 流程图支持两种可切换视角：
    - `byProduct`：以产物流转为主线组织节点与连线。
    - `byBuilding`：以设备/建筑为主线组织节点与连线。
  - 用户可在同一结果上下文中切换视角，切换后保持同一组规划输入与计算结果，不触发重算。
- 非目标（可选）：
  - 本需求不包含自动布局算法优化与美化主题扩展。
  - 本需求不改变现有配方求解逻辑与结果数值。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：新增流程图容器、节点渲染、视角切换控件。
  - `domain`：补充流程图投影视图模型（按产物/按设备映射）。
  - `data`：新增视图偏好持久化键（如当前视角）。
  - `test`：新增两种视角渲染一致性与切换稳定性回归。
  - `sim`：无直接变更（只读消费既有结果）。
- 依赖项：
  - 规划器结果结构需可稳定映射为节点与边。
  - 既有树状列表数据源需复用，避免双口径。
- 风险与回滚点：
  - 风险：双视角映射规则不一致导致信息不对齐。
  - 风险：大规模结果渲染性能抖动影响交互。
  - 回滚点：保留树状列表为默认展示，流程图能力可按开关整体下线。
- 验收标准（DoD）：
  1. 规划器结果页可展示流程图，且与树状列表可并存。
  2. 支持 `byProduct` 与 `byBuilding` 双视角切换，切换无报错且不触发规划重算。
  3. 同一输入下，两种视角与树状列表在关键产物与关键设备集合上信息一致。
  4. 视角选择可持久化，刷新后保持用户上次选择。
  5. Stage2 冻结主流程与 Stage3 已有验收项无 P0/P1 回归。

### 3.2 S3-RQ-009 全设备功耗字段补齐（领域模型）

- 状态：`todo`
- 业务价值：
  - 当前设备功耗数据不完整会导致电力统计与设备级观测失真。
  - 补齐全设备功耗字段可为后续电力计算、可视化与平衡调优提供统一数据基线。
- 详细需求：
  - 在领域模型中为所有设备补齐 `powerDemand`（消耗功率）数据。
  - 要求设备配置中显式可读，不依赖运行时隐式推导。
  - 对不消耗电力设备，`powerDemand` 显式为 `0`。
  - 发电设备在设备属性页展示 `powerDemand=0`。
- 非目标（可选）：
  - 本需求不包含发电设备供电曲线建模细化。
  - 本需求不包含 UI 样式改造。
- 影响面（domain/sim/ui/data/test）：
  - `domain`：设备配置结构与设备注册表数据补齐。
  - `sim`：读取设备功耗口径时改为统一消费 `powerDemand`。
  - `ui`：设备详情与电力看板可消费完整字段。
  - `test`：补充配置完整性校验与关键设备功耗回归。
  - `data`：无新增持久化结构（仅配置数据补齐）。
- 依赖项：
  - 设备清单口径稳定（含武陵与基础区域设备）。
  - 电力字段契约与默认值策略在 Stage3 内达成一致。
- 风险与回滚点：
  - 风险：批量录入功耗值时单位或量纲不一致。
  - 风险：旧设备缺失字段导致运行时 fallback 偏差。
  - 回滚点：保留旧口径兼容读取，按特性开关切换到补齐字段。
- 验收标准（DoD）：
  1. 领域模型设备配置中“所有可放置设备”均具备可解析的 `powerDemand`。
  2. 缺失字段率为 0（按设备注册表全量扫描）。
  3. 不耗电设备口径一致且在文档中明确（`powerDemand=0`）。
  4. 发电设备在属性页显示 `powerDemand=0`。
  5. 电力聚合结果在引入全量字段后与预期对账通过。
  6. Stage2 冻结主流程与 Stage3 已有验收项无 P0/P1 回归。

### 3.3 S3-RQ-010 自动布线（Auto Routing）独立模块

- 状态：`todo`
- 业务价值：
  - 降低产线规划后手动连接成本，缩短从“规划结果”到“可执行布局蓝图”的路径。
  - 将耗时生成过程异步化，避免阻塞前台交互。
  - 通过独立模块 + 任务队列形态，为后续批量生成与调度扩展提供基础。
- 详细需求（已补充）：
  - 新增全新功能模块“自动布线（Auto Routing）”，作为后台任务系统运行。
  - UI 增加“任务队列”入口按钮，位置在“规划器按钮旁边”。
  - 在规划器内部提供“发送到队列”操作：当用户配置好一份规划后，可将该规划提交到后台队列。
  - 后台队列按任务消费规划输入，自动生成一份“摆放蓝图”。
  - 生成蓝图必须可放置在“当前基地尺寸”内；当前阶段仅考虑基地尺寸边界约束。
  - 任务参数必须包含基地尺寸（宽/高或等效边界定义），并作为生成约束输入。
  - 自动布线模块在架构上与其他功能模块解耦，保持独立边界。
  - 自动布线模块只读取领域模型，不依赖其他模块运行态。
  - 任务队列需具备基础状态流转：`queued` → `running` → `done | failed | canceled`。
  - 蓝图生成过程允许长时运行，任务队列需提供可观测进度信息（至少在 `running` 阶段可见进度）。
- 输出定义：
  - 每个成功任务（`done`）必须满足“所有连接均正确路由完成”，并产出一份可被系统识别的“摆放蓝图结果对象”（具体字段后续在 API 文档细化）。
- 非目标（可选）：
  - 不与现有模块形成双向依赖。
  - 不改写既有仿真核心规则。
  - 本需求不包含自动布线算法“最优解保证”。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：
    - 顶部新增任务队列入口（位于规划器按钮旁）。
    - 规划器内新增“发送到队列”触发按钮。
    - 新增任务队列面板（展示任务状态与蓝图产出）。
  - `domain`：读取领域模型实体、配方与连接约束（只读）。
  - `sim`：默认无直接耦合，按独立模块落地。
  - `data`：新增任务队列任务实体与状态持久化（仅会话级；关闭浏览器后全部丢失）。
  - `test`：补充入队、状态流转、失败恢复、蓝图产出一致性、尺寸约束与进度上报测试。
- 依赖项：
  - 领域模型数据口径稳定且可被只读消费。
  - 规划器触发协议、规划快照结构与队列任务载荷格式明确。
  - 基地尺寸参数定义与任务载荷字段命名达成一致。
- 风险与回滚点：
  - 风险：入口在规划器但模块独立，若边界定义不清易形成隐式耦合。
  - 风险：后台任务积压导致长时间等待或状态不一致。
  - 风险：尺寸约束处理不完整导致蓝图越界。
  - 风险：蓝图生成失败时缺乏可诊断信息影响可用性。
  - 回滚点：保持入口与队列面板可开关下线，异常时退化为仅保留规划器原有流程。
- 验收标准（DoD）：
  1. 顶栏存在任务队列入口，且位置在规划器按钮旁边。
  2. 规划器内可将“当前规划”发送到自动布线后台队列。
  3. 任务状态按 `queued` → `running` → `done | failed | canceled` 可观测。
  4. 任务参数包含基地尺寸，且生成结果满足“可放置在当前基地尺寸内”。
  5. `running` 任务可查看进度信息（如百分比或阶段进度）。
  6. `done` 任务必须满足“所有连接均正确路由完成”；`failed` 任务可观测失败状态与失败原因。
  7. 自动布线模块代码结构独立，且计算仅依赖领域模型只读数据。
  8. Stage2 冻结主流程与 Stage3 已有验收项无 P0/P1 回归。

### 3.4 S3-RQ-012 公共蓝图目录与构建索引

- 状态：`partial`
- 业务价值：
  - 提供开箱可用的公共蓝图列表，降低首次使用门槛。
  - 将“蓝图资源发现”从运行时临时输入改为构建期可控产物，提升稳定性与可维护性。
- 详细需求（首版）：
  - 公共蓝图供给流程为：用户导出有效蓝图文件 -> 人工放置到 `public/blueprints` -> 执行 `npm run gen:blueprint-index` 生成索引 -> 需要发布时再执行网站构建。
  - 在 `public` 下新增 `blueprints` 目录。
  - 索引仅收录 `.json` 蓝图文件。
  - 通过独立命令 `npm run gen:blueprint-index` 生成目录索引文件（例如 `public/blueprints/index.json`）。
  - 索引生成命令不与网站 `build/dev` 流程绑定。
  - 在蓝图模式新增“公共蓝图”版块。
  - 每次进入蓝图模式时异步读取索引（索引至少包含 `id`、`version`）。
  - 若本地存在同 `id` 且同 `version`，不读取蓝图正文。
  - 仅当“新增蓝图”或“蓝图升级”时，才读取蓝图正文并同步到本地。
  - 本地“系统蓝图”需与索引保持一致：本地多余项要删除。
  - 本地需区分“用户蓝图”与“系统蓝图”。
  - ID 规范：用户蓝图 `BluePrint-HSY-<UUID>`；系统蓝图 `PublicBluePrint-HSY-<UUID>`。
- 非目标（可选）：
  - 不包含远程同步与在线更新。
  - 不包含公共蓝图编辑回写到资源目录。
  - 不包含运行时上传并自动写入 `public/blueprints`。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：蓝图模式新增“公共蓝图”列表区与状态展示。
  - `data`：新增公共蓝图索引缓存读取与本地持久化。
  - `scripts/build`：新增构建期索引生成脚本与构建流程接入。
  - `domain`：可选补充蓝图元数据类型定义（如 `BlueprintIndexEntry`）。
  - `sim`：无直接变更。
  - `test`：新增索引生成、启动读取、缓存回退与异常场景测试。
- 依赖项：
  - 蓝图文件格式与白名单约定（如 `.json`）。
  - 蓝图模式现有状态管理入口与持久化机制。
- 风险与回滚点：
  - 风险：索引与目录文件不一致导致列表不可用。
  - 风险：缓存与索引版本漂移引起展示旧数据。
  - 回滚点：公共蓝图版块可按开关下线，失败时退化到本地蓝图流程。
- 验收标准（DoD）：
  1. `public/blueprints` 目录存在且可放置蓝图文件。
  2. 每次执行 `npm run gen:blueprint-index` 后可生成并访问蓝图索引文件，且只包含 `.json` 蓝图。
  3. 每次进入蓝图模式时异步请求索引，不对所有蓝图正文做全量读取。
  4. 同 `id` 同 `version` 的蓝图不重复拉取正文。
  5. 新增或升级蓝图会拉取正文并同步本地。
  6. 本地系统蓝图集合始终与索引一致（多余项会删除）。
  7. 本地可区分用户蓝图与系统蓝图。
  8. ID 前缀规范可防止自动生成同 `id` 冲突。
  9. 公共蓝图版块可展示本地同步后的列表。
  10. 索引缺失或读取失败不阻断应用主流程。
  11. 手工新增蓝图文件需重新执行索引命令后才能被索引与展示。

- 实现情况（2026-03-04）：
  - 已完成：独立索引命令 `npm run gen:blueprint-index` 与脚本 `scripts/generate-blueprint-index.mjs`。
  - 已完成：索引仅扫描 `.json`，并写出 `public/blueprints/index.json`。
  - 已完成：蓝图模式进入时异步同步索引；按 `id+version` 增量拉取正文；系统蓝图按索引强一致删除。
  - 已完成：本地存储分层为“用户蓝图（user）/系统蓝图（system）”，并提供 Stage1 -> Stage3 迁移入口。
  - 已完成：蓝图面板分组展示“我的蓝图（上）/公共蓝图（下）”，公共蓝图分组常驻并支持空文案。
  - 已完成：ID 规范落地（`BluePrint-HSY-<UUID>` / `PublicBluePrint-HSY-<UUID>`）并接入生成/迁移/同步。
  - 待补：端到端自动化回归用例尚未补齐（当前以构建验证与手动验证为主）。

### 3.5 S3-RQ-013 放置模式 Shift 连续放置

- 状态：`done`
- 业务价值：
  - 当前每次放置后回退到选择模式，批量建造操作成本高、节奏被频繁打断。
  - 增加按键增强后可显著降低重复切换操作，提升建造效率与手感一致性。
- 详细需求：
  - 在放置模式下，用户按住 `Shift` 执行放置时，放置成功后保持在放置模式，不自动回退到选择模式。
  - 不按 `Shift` 时保持现有行为（放置后回退到选择模式）。
  - 连续放置期间，沿用当前已选建筑类型与当前旋转/朝向设置。
  - 仅在“放置成功”时触发连续放置保持；若放置失败（越界/冲突等），行为沿用现有失败处理口径。
- 非目标（可选）：
  - 本需求不引入“锁定连续放置”开关。
  - 本需求不改动热键体系与其他模式切换规则。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：放置交互状态机增加 `Shift` 条件分支。
  - `domain`：无新增领域字段。
  - `sim`：无新增仿真规则。
  - `data`：无新增持久化结构。
  - `test`：新增放置成功/失败与 `Shift` 按下/未按下组合回归。
- 依赖项：
  - 放置模式与选择模式现有切换逻辑稳定可复用。
  - 键盘状态读取在放置提交时机可用。
- 风险与回滚点：
  - 风险：键盘状态判定时机不一致导致“偶发未保持放置模式”。
  - 风险：与既有快捷键冲突造成误触。
  - 回滚点：通过特性开关或逻辑分支快速回退到“放置后总是回退选择模式”的旧行为。
- 验收标准（DoD）：
  1. 放置模式下按住 `Shift` 连续放置，成功放置后不会自动回退到选择模式。
  2. 放置模式下不按 `Shift`，成功放置后行为与当前版本一致（回退到选择模式）。
  3. 连续放置期间建筑类型与旋转/朝向保持不变。
  4. 放置失败场景不引入新异常，且不破坏当前失败提示与处理流程。
  5. Stage2 冻结主流程与 Stage3 已有验收项无 P0/P1 回归。

- 实现情况（2026-03-04）：
  - 已完成：放置成功后仅在未按 `Shift` 时回退到选择模式；按住 `Shift` 保持当前放置类型。
  - 已完成：放置失败分支维持原有行为，无新增异常处理分叉。
  - 已完成：本地构建通过（`npm run build`）。

### 3.6 S3-RQ-014 物品选择器最近选择分组与模块化

- 状态：`done`
- 业务价值：
  - 取货口/预放置物品选择存在高频重复选择，最近项可降低重复查找成本。
  - 将选择器契约独立化可减少跨模块重复定义，降低后续维护成本。
- 详细需求：
  - 物品选择对话框顶部新增“最近选择”分组，仅显示一行（当前一行容量）。
  - 下方保留“全部物品”分组，覆盖完整可选集合。
  - 最近选择需与当前过滤条件联动，仅展示当前上下文可选物品。
  - 对话框相关状态/过滤类型抽离为独立代码模块，供 UI 与 domain 共用。
- 非目标（可选）：
  - 不引入搜索框、分页或额外筛选器。
  - 不改动既有物品选择提交语义。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：对话框新增分组标题与最近项单行展示。
  - `domain`：无新增领域规则。
  - `sim`：无直接变更。
  - `data`：新增最近选择本地持久化键。
  - `test`：建议补充分组渲染与最近项更新回归（当前以构建+手测为主）。
- 依赖项：
  - 物品图标与名称映射能力稳定。
  - 选择器上下文过滤规则（pickup/pump/preload）可复用。
- 风险与回滚点：
  - 风险：最近项与过滤规则不一致导致显示越权条目。
  - 风险：类型定义分散造成维护漂移。
  - 回滚点：可快速退回到仅“全部物品”单分组渲染。
- 验收标准（DoD）：
  1. 对话框顶部存在“最近选择”分组且仅占一行。
  2. 对话框下方存在“全部物品”分组并可完整选择。
  3. 最近项仅显示当前过滤后仍可选物品。
  4. 最近项会在选择后更新并持久化。
  5. 选择器共享类型独立成单文件并被调用方复用。
  6. Stage2 冻结主流程与 Stage3 已有验收项无 P0/P1 回归。

- 实现情况（2026-03-04）：
  - 已完成：新增“最近选择/全部物品”双分组；最近分组固定单行容量（当前 8）。
  - 已完成：新增最近选择持久化键 `stage3-item-picker-recent-item-ids`（去重、按最近时间排序）。
  - 已完成：抽离 `src/ui/dialogs/itemPicker.types.ts` 作为选择器共享契约。
  - 已完成：中英文文案与样式同步更新；本地构建通过（`npm run build`）。

### 3.7 S3-RQ-015 蓝图复制/生成最低建筑门槛调整

- 状态：`done`
- 业务价值：
  - 单建筑调试与模板化保存是高频场景，门槛降低可减少无效操作阻塞。
  - 与“保存蓝图已支持单建筑”口径统一，避免用户心智不一致。
- 详细需求：
  - 复制为临时蓝图（`Ctrl+C`）最低选中门槛改为 `1`。
  - 保存为蓝图口径明确为“选中 `1` 个及以上建筑即可”。
  - 相关提示文案与帮助文档同步更新，避免旧口径残留。
- 非目标（可选）：
  - 不改动蓝图数据结构与放置逻辑。
  - 不改动多选框选机制。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：快捷键拦截阈值与提示文案更新。
  - `domain`：无直接变更。
  - `sim`：无直接变更。
  - `data`：无新增持久化结构。
  - `test`：建议补充 `0/1/N` 选中数量路径回归（当前以构建+手测为主）。
- 依赖项：
  - 现有选中集合与基础过滤（排除地基）逻辑可复用。
- 风险与回滚点：
  - 风险：文档与实现口径不一致导致误导。
  - 回滚点：可回退快捷键阈值到旧值，不影响蓝图存储结构。
- 验收标准（DoD）：
  1. 选中 1 个建筑时可执行 `Ctrl+C` 复制为临时蓝图。
  2. 选中 0 个建筑时仍给出拦截提示。
  3. 保存蓝图在选中 1 个建筑时可成功创建。
  4. 中英文提示与帮助文档与新口径一致。
  5. Stage2 冻结主流程与 Stage3 已有验收项无 P0/P1 回归。

- 实现情况（2026-03-04）：
  - 已完成：`Ctrl+C` 复制阈值由“至少 2 个”调整为“至少 1 个”。
  - 已完成：中英文 toast 文案与 Wiki 快捷键文案同步为新口径。
  - 已核对：保存蓝图原逻辑已满足“至少 1 个”，无需额外代码改动。
  - 已完成：本地构建通过（`npm run build`）。

### 3.8 S3-RQ-016 供电桩放置预览供电范围与受电建筑高亮

- 状态：`done`
- 业务价值：
  - 放置供电桩时可提前感知覆盖边界，减少“落地后才发现覆盖不足”的返工。
  - 实时高亮受影响建筑可提升供电网络规划效率与可读性。
- 详细需求：
  - 当处于放置模式且当前建筑为供电桩时，在虚影状态展示该供电桩的供电范围边界。
  - 在该供电范围内、且 `requiresPower=true` 的已放置建筑，追加白边高亮。
  - 仅在供电桩放置预览期间生效，不改变其他放置模式行为。
- 非目标（可选）：
  - 不改动仿真供电判定规则。
  - 不引入新面板或额外交互开关。
- 影响面（domain/sim/ui/data/test）：
  - `ui`：世界层新增供电桩预览边界样式与建筑白边高亮样式。
  - `domain`：无新增字段。
  - `sim`：无逻辑变更。
  - `data`：无新增持久化结构。
  - `test`：通过构建验证；建议后续补充交互回归用例。
- 验收标准（DoD）：
  1. 放置供电桩时可见供电范围边界预览。
  2. 范围内 `requiresPower=true` 的建筑显示白边。
  3. 切换到非供电桩放置模式后，高亮与预览边界消失。
  4. 不影响 Stage2 冻结主流程与 Stage3 已有验收项。

- 实现情况（2026-03-04）：
  - 已完成：供电桩虚影状态新增供电范围预览边界。
  - 已完成：范围内需电建筑新增白边高亮。
  - 已完成：白边高亮可见性增强（加粗边框 + 内外发光 + 外轮廓），复杂贴图下可读性提升。
  - 已完成：本地构建通过（`npm run build`）。

### 3.X S3-RQ-XXX 需求标题

- 状态：`todo | partial | done`
- 业务价值：
- 详细需求：
- 非目标（可选）：
- 影响面（domain/sim/ui/data/test）：
- 依赖项：
- 风险与回滚点：
- 验收标准（DoD）：

## 4. 执行节奏建议

- M1：电力模型与语义改造（S3-RQ-001/002/009）
- M2：Stage2/Stage3 回归收口（含反应池 5 共享槽位基线）
- M3：产线规划器流程图与双视角切换（S3-RQ-008）
- M4：自动布线独立模块落地（S3-RQ-010，Stage3 必做）

## 5. 追踪约定

- 状态取值：`todo`、`partial`、`done`、`blocked`
- 需求编号稳定，不因排序变化而修改
- 任一需求进入开发前，必须补齐 DoD 与影响面
