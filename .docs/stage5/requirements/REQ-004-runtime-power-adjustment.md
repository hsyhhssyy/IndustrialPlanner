# S5-RQ-004 仿真运行时动态调整能耗

## 文档内容范围、结构与格式约定（必读）

### 1. 本文档负责什么

本文档用于沉淀 S5-RQ-004 上的全部结构化记忆，并采用“状态视图 + 时间线附录”的混合写法，只负责本需求相关的：

1. 运行中动态调整能耗的目标能力。
2. 相关的交互口径、仿真影响、统计反馈。
3. 验收、测试、异常与修复记录。

本文档不负责：

1. 记录所有仿真系统的通用设计。
2. 替代图表需求记录可视化细节。
3. 记录逐字聊天内容。

### 2. 本文档固定结构

1. 文档内容范围、结构与格式约定
2. 需求卡片
3. 背景与目标
4. 当前口径
5. 当前状态视图
6. 功能拆解
7. 验收标准
8. 测试记录
9. bug / 修复记录
10. 中断与恢复记录
11. 需求拉扯与决策时间线
12. 待确认问题
13. 状态更新

### 3. 本文档记录格式

1. 文档前半部分优先总结当前方案、当前实现缺口与下一步。
2. 功能口径要明确区分“运行前配置”和“运行中调整”。
3. 涉及统计影响时，应同时说明设备状态、电力统计、电池变化是否同步。
4. 如存在临时实现，应明确写出限制条件。
5. 若开发中被其他 bug 打断，先登记到 [../bugs.md](../bugs.md)，再在本需求记录影响。

## 需求卡片

- 需求编号：`S5-RQ-004`
- 名称：仿真运行时动态调整能耗
- 优先级：`P1`
- 状态：`todo`
- 所属里程碑：`M2`
- 关联总表： [requirements-index.md](../requirements-index.md)

## 背景与目标

用户需要在仿真已经运行时直接调整能耗或相关参数，并立即观察到设备状态、电力统计与电池变化，而不必重开仿真。

## 当前口径

1. 运行中调整后应即时生效。
2. 调整结果必须反映到仿真状态与电力统计。
3. 若有图表展示，则图表结果必须与底层统计一致。

## 当前状态视图

- 当前状态：`todo`
- 当前目标：明确可调参数范围与运行中生效链路。
- 当前已知进展：已确认核心验收点，尚未确定具体交互入口。
- 下一步动作：
	1. 明确参数范围；
	2. 确定 UI 入口；
	3. 打通仿真更新与统计刷新。

## 功能拆解

1. 明确可被动态调整的参数范围。
2. 确定运行中修改入口与交互反馈。
3. 接通仿真引擎内的参数更新链路。
4. 同步刷新设备状态、电力统计、电池变化及相关图表。

## 验收标准

1. 运行中修改能耗后无需重开仿真即可生效。
2. 设备运行反馈与统计结果一致。
3. 总耗电、总发电、净功率、电池变化与底层仿真口径一致。
4. 不因动态修改造成明显卡死、状态错乱或统计滞后。

## 测试记录

> 当前为空。后续至少覆盖单设备、多设备、带电池场景。

## bug / 修复记录

> 当前为空。后续按条目追加。

## 中断与恢复记录

> 当前为空。后续记录开发中断、测试往返与恢复节点；跨需求问题同步到 [../bugs.md](../bugs.md)。

## 需求拉扯与决策时间线

### 2026-03-09

1. 确认该需求强调“运行时”而非“预配置”。
2. 确认无需重启仿真即可生效是核心验收点。
3. 确认统计一致性是本需求不可缺失的一部分。

## 待确认问题

1. 当前允许调整的是单设备能耗、设备类别参数，还是全局倍率，尚待实现前明确。
2. UI 入口的具体形态仍待设计。

## 状态更新

- 2026-03-09：建立需求文档，待补充具体交互方案与技术路径。
