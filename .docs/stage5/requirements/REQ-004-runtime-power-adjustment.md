# S5-RQ-004 仿真运行时动态调整能耗

## 文档内容范围、结构与格式约定（必读）

### 1. 本文档负责什么

本文档用于沉淀 S5-RQ-004 上的全部结构化记忆，并采用“状态视图 + 时间线附录”的混合写法，只负责本需求相关的：

1. 运行中动态调整能耗的目标能力。
2. 相关的交互口径、仿真影响、统计反馈。
3. 验收、测试、异常与修复记录。

本文档不负责：

1. 记录所有仿真系统的通用设计。
2. 替代图表需求记录可视化细节。
3. 记录逐字聊天内容。

### 2. 本文档固定结构

1. 文档内容范围、结构与格式约定
2. 需求卡片
3. 背景与目标
4. 当前口径
5. 当前状态视图
6. 功能拆解
7. 验收标准
8. 测试记录
9. bug / 修复记录
10. 中断与恢复记录
11. 需求拉扯与决策时间线
12. 待确认问题
13. 状态更新

### 3. 本文档记录格式

1. 文档前半部分优先总结当前方案、当前实现缺口与下一步。
2. 功能口径要明确区分“运行前配置”和“运行中调整”。
3. 涉及统计影响时，应同时说明设备状态、电力统计、电池变化是否同步。
4. 如存在临时实现，应明确写出限制条件。
5. 若开发中被其他 bug 打断，先登记到 [../bugs.md](../bugs.md)，再在本需求记录影响。

## 需求卡片

- 需求编号：`S5-RQ-004`
- 名称：仿真运行时动态调整能耗
- 优先级：`P1`
- 状态：`in-progress`
- 所属里程碑：`M2`
- 关联总表： [requirements-index.md](../requirements-index.md)

## 背景与目标

用户需要在仿真已经运行时直接调整能耗或相关参数，并立即观察到设备状态、电力统计与电池变化，而不必重开仿真。

## 当前口径

1. 运行中调整后应即时生效。
2. 调整结果必须反映到仿真状态与电力统计。
3. 若有图表展示，则图表结果必须与底层统计一致。
4. 当前实现口径先采用“全局总耗电覆盖值”：用户输入一个总耗电数值后，不再跟随当前地图设备累计耗电。
5. 交互入口放在右侧面板顶部仿真全局控制区，与“电力模式”同区域，放置态与仿真态都可调整。
6. 输入留空时回退为真实设备累计耗电；界面同时显示真实值作为参考。
7. 仿真运行中修改的覆盖值只对当前这次仿真生效；停止仿真后恢复为进入仿真前的设置值。
8. 左侧“仿真调试”中的耗电 / 发电摘要下方，当前新增一个默认收起的“展开平均功率”区域；展开后显示 `1小时 / 30分钟 / 10分钟` 三档平均发电功率。
9. 上述平均发电功率采用仿真 tick 窗口滚动统计；若仿真运行时长尚未覆盖完整窗口，则按当前已运行区间求平均。

## 当前状态视图

- 当前状态：`in-progress`
- 当前目标：完成全局总耗电覆盖值链路，并补齐运行态验证与电力观测信息。
- 当前已知进展：已明确参数范围、交互入口与停止仿真后的恢复口径；首版代码已接通 UI 与仿真总耗电计算；并已补齐一个联调中暴露出的底层电力 bug：发电富余时会重新回充蓄电池；当前左侧“仿真调试”已补充首版平均发电功率展开区，作为后续电力曲线图前的轻量观测形态。
- 下一步动作：
	1. 完成运行态手工回归；
	2. 校验供电不足与电池变化口径；
	3. 视后续图表需求决定是否将当前平均功率展开区升级为真正的电力曲线图。

## 功能拆解

1. 提供全局总耗电覆盖值输入框，支持空值回退到真实总耗电。
2. 在右侧顶部全局控制区展示真实总耗电、覆盖值与当前生效值。
3. 接通仿真引擎内的运行时更新链路，使覆盖值在运行中修改后直接生效。
4. 同步刷新设备状态、电力统计与电池变化；后续图表需求必须沿用同一底层口径。
5. 在左侧“仿真调试”中提供默认收起的平均发电功率展开区，复用已有展开交互样式，展示 `1小时 / 30分钟 / 10分钟` 三档滚动平均值。

## 验收标准

1. 运行中修改能耗后无需重开仿真即可生效。
2. 设备运行反馈与统计结果一致。
3. 总耗电、总发电、净功率、电池变化与底层仿真口径一致。
4. 不因动态修改造成明显卡死、状态错乱或统计滞后。
5. “展开平均功率”区域默认收起，展开后显示 `1小时 / 30分钟 / 10分钟` 三档平均发电功率，且数值与仿真滚动窗口口径一致。

## 测试记录

### 2026-03-10

1. 执行 `npm run build`，构建通过。
2. 手工运行态验证尚未完成，待补充以下场景：
	- 无覆盖值 / 有覆盖值切换；
	- 覆盖值大于真实耗电时的低电状态与电池下降；
	- 停止仿真后恢复进入仿真前设置值。

### 2026-03-11

1. 检查电池行为时确认：原实现支持供电不足时放电，但未支持发电富余时回充，属于底层电力结算缺口。
2. 已在供电结算中补齐富余发电量回充蓄电池的逻辑，使电池变化重新与供需平衡一致。

### 2026-03-11：富余发电量回充构建验证

- 时间：2026-03-11
- 分类：电力 / 电池
- 场景：供电不足阶段电池先放电，发电恢复后出现富余功率时电池应重新充电
- 核对对象：`buildPowerAvailabilityByDeviceId()` 中的电池结算逻辑
- 结果：已补齐 surplus 回充分支，`npm run build` 通过
- 证据：本轮对话内构建结果

### 2026-03-11：平均发电功率展开区构建验证

- 时间：2026-03-11
- 分类：观测 / 电力统计
- 场景：在左侧“仿真调试”中为耗电 / 发电摘要补充一个默认收起的“展开平均功率”区域
- 操作步骤：在仿真循环内新增按 tick 记录的发电功率滚动窗口；在左侧观测区复用现有展开按钮样式，显示 `1小时 / 30分钟 / 10分钟` 平均发电功率；随后执行 `npm run build`
- 预期结果：展开区默认收起；展开后展示 3 档平均发电功率；构建通过
- 实际结果：构建通过
- 证据：本轮对话内代码改动与构建结果

## bug / 修复记录

### 2026-03-11：富余发电量未回充蓄电池

- 分类：兼容逻辑 / 统计
- 标题：发电恢复后蓄电池不会回充
- 现象：低电阶段电池会下降，但发电恢复后即便出现净正功率，电池也不回升
- 根因：电力结算只处理 deficit 放电，缺少 surplus 回充分支
- 修复：补齐将富余发电量换算为焦耳并写回 `batteryStoredJ` 的逻辑，同时受全局电池容量限制
- 回归：代码已修复并通过构建，待用户联调确认
- 状态：`fixed`

## 中断与恢复记录

> 当前为空。后续记录开发中断、测试往返与恢复节点；跨需求问题同步到 [../bugs.md](../bugs.md)。

## 需求拉扯与决策时间线

### 2026-03-09

1. 确认该需求强调“运行时”而非“预配置”。
2. 确认无需重启仿真即可生效是核心验收点。
3. 确认统计一致性是本需求不可缺失的一部分。

### 2026-03-10

1. 明确本轮先做“全局总耗电覆盖值”，而不是单设备或分类参数。
2. 明确交互入口位于右侧面板顶部仿真全局控制区。
3. 明确覆盖值放置态与仿真态都可编辑。
4. 明确运行中修改只作用于当前仿真，停止仿真后恢复到进入仿真前设置值。
5. 实现上采用“总耗电覆盖 + 真实值参考显示”的首版方案。

### 2026-03-11

1. 用户提出想做“电力曲线图”，但当前先接受一个更轻量的阶段性形态：在左侧“仿真调试”中增加可展开的平均发电功率区域。
2. 确认该区域默认收起，标题沿用“展开平均功率”，展开后先展示 `1小时 / 30分钟 / 10分钟` 三档平均发电功率。
3. 确认交互样式复用当前已有的“展开性能详情”按钮口径，不额外引入新的复杂控件。

## 待确认问题

1. 当前尚未完成运行态手工验收。
2. 若后续需要更细粒度控制，再评估是否补充单设备 / 分类 / 全局倍率的二层能力。

## 状态更新

- 2026-03-09：建立需求文档，待补充具体交互方案与技术路径。
- 2026-03-10：将需求状态更新为 `in-progress`，明确“全局总耗电覆盖值”口径，并完成首版 UI / 仿真链路接入。
- 2026-03-11：补齐电力结算中的蓄电池回充逻辑；当前在发电富余时会将多余功率回充进蓄电池，待用户联调确认。
- 2026-03-11：左侧“仿真调试”新增默认收起的“展开平均功率”区域，展开后显示 `1小时 / 30分钟 / 10分钟` 平均发电功率，作为后续电力曲线图前的首版观测能力。
