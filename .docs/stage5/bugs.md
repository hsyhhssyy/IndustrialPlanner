# Stage5 全局 bug / 中断记录

## 文档内容范围、结构与格式约定（必读）

### 1. 本文档负责什么

本文档用于记录游离于单个编号需求之外，或暂时不适合直接归档到单需求文档中的 bug 与中断事件。

本文档只负责：

1. 记录跨需求 bug。
2. 记录开发过程中临时爆发、会打断当前需求推进的问题。
3. 记录暂时无法明确归属的历史遗留 bug。
4. 为 bug 与需求之间建立双向链接。

本文档不负责：

1. 代替单需求文档保存该需求的完整业务上下文。
2. 记录所有测试细节。
3. 记录逐字聊天过程。

### 2. 本文档固定结构

本文档必须长期保持以下结构：

1. 文档内容范围、结构与格式约定
2. 使用规则
3. bug / 中断总表
4. 条目明细
5. 更新日志

### 3. 本文档记录格式

每个条目必须使用以下字段：

- `BUG-ID`：唯一编号，格式 `S5-BUG-XXX`
- `标题`
- `发现时间`
- `发现来源`
- `当前状态`：`open`、`in-progress`、`blocked`、`fixed`、`closed`
- `归属类型`：`independent`、`related-to-requirement`、`cross-requirement`、`unknown`
- `关联需求`
- `现象 / 影响`
- `处理结果`
- `回归情况`

## 使用规则

1. 开发 A 需求时若被 bug 打断，先在本文档登记，再决定是否同步写入 A 或 B 的需求文档。
2. 若 bug 明确只属于某个需求，仍可主要记录在对应需求文档；但若它打断了多个需求或后续需要追踪来源，建议同时登记在本文档。
3. bug 修复完成后，若影响到某个需求状态，必须同步更新对应需求文档与 [requirements-index.md](requirements-index.md)。
4. 本文档优先解决“先落地记录，避免上下文丢失”的问题；归属可以后补，不要求首次登记时完全准确。

## bug / 中断总表

| BUG-ID | 标题 | 发现时间 | 当前状态 | 归属类型 | 关联需求 |
| --- | --- | --- | --- | --- | --- |
| S5-BUG-001 | 端口优先级组菜单撑大对话框并出现显示/层级问题 | 2026-03-09 | closed | cross-requirement | S5-RQ-001, S5-RQ-005 |
| S5-BUG-002 | 无限电力模式错误绕过供电范围 | 2026-03-09 | closed | cross-requirement | S5-RQ-001, S5-RQ-005 |
| S5-BUG-003 | 协议存储箱在供电范围外仍执行自动提交 | 2026-03-09 | closed | cross-requirement | S5-RQ-001, S5-RQ-005 |
| S5-BUG-004 | 选中详情面板失去滚动能力，导致下方内容不可见 | 2026-03-10 | fixed | cross-requirement | S5-RQ-001, S5-RQ-005 |
| S5-BUG-005 | 电力恢复后多余发电量未回充蓄电池 | 2026-03-11 | fixed | cross-requirement | S5-RQ-001, S5-RQ-004 |

## 条目明细

### `S5-BUG-001`

- `BUG-ID`：`S5-BUG-001`
- `标题`：端口优先级组菜单撑大对话框并出现显示/层级问题
- `发现时间`：2026-03-09
- `发现来源`：实现 `S5-RQ-005` 联调过程中，由用户反馈
- `当前状态`：`closed`
- `归属类型`：`cross-requirement`
- `关联需求`：`S5-RQ-001`、`S5-RQ-005`
- `现象 / 影响`：端口优先级组选择菜单会撑大弹窗导致滚动条；替换方案一度又出现触发器显示不全，以及菜单被弹窗层压住的问题
- `处理结果`：最终改为“自定义触发器 + portal 浮层菜单 + 独立滚动舞台”，并提高菜单层级、增大触发器宽度
- `回归情况`：用户最终确认“没问题了”
- `备注 / 回链`：详见 [requirements/REQ-001-bug-and-stability.md](requirements/REQ-001-bug-and-stability.md) 与 [requirements/REQ-005-item-admission.md](requirements/REQ-005-item-admission.md)

### `S5-BUG-002`

- `BUG-ID`：`S5-BUG-002`
- `标题`：无限电力模式错误绕过供电范围
- `发现时间`：2026-03-09
- `发现来源`：实现 `S5-RQ-005` 过程中用户补充反馈
- `当前状态`：`closed`
- `归属类型`：`cross-requirement`
- `关联需求`：`S5-RQ-001`、`S5-RQ-005`
- `现象 / 影响`：在无限电力模式下，供电范围外的精炼炉等设备仍可运行，破坏仿真正确性
- `处理结果`：修正供电语义，保留供电范围校验；无限电力仅表示供电能力无穷，不再绕过覆盖范围
- `回归情况`：用户明确反馈“另外两个已经验证修复”中的其一
- `备注 / 回链`：详见 [requirements/REQ-001-bug-and-stability.md](requirements/REQ-001-bug-and-stability.md)

### `S5-BUG-003`

- `BUG-ID`：`S5-BUG-003`
- `标题`：协议存储箱在供电范围外仍执行自动提交
- `发现时间`：2026-03-09
- `发现来源`：实现 `S5-RQ-005` 过程中用户补充反馈
- `当前状态`：`closed`
- `归属类型`：`cross-requirement`
- `关联需求`：`S5-RQ-001`、`S5-RQ-005`
- `现象 / 影响`：协议存储箱在供电范围外仍上传物品，和“只有自动提交受供电范围影响”的正确口径不一致
- `处理结果`：仅让自动提交分支依赖供电范围，保留其他功能正常可用
- `回归情况`：用户明确反馈“另外两个已经验证修复”中的其一
- `备注 / 回链`：详见 [requirements/REQ-001-bug-and-stability.md](requirements/REQ-001-bug-and-stability.md)

### `S5-BUG-004`

- `BUG-ID`：`S5-BUG-004`
- `标题`：选中详情面板失去滚动能力，导致下方内容不可见
- `发现时间`：2026-03-10
- `发现来源`：Stage5 持续联调时，由用户反馈
- `当前状态`：`fixed`
- `归属类型`：`cross-requirement`
- `关联需求`：`S5-RQ-001`、`S5-RQ-005`
- `现象 / 影响`：右侧选中详情在内容较长时无法滚动，导致下半部分配置与状态信息不可见
- `处理结果`：排查发现并非历史长期缺失，而是 2026-03-09 引入可折叠侧栏时新增了 `panel-slot` 包裹层，导致内部 `.panel` 不再被约束到槽位高度，`overflow: auto` 失效；已改为让 `panel-slot` 作为 flex 容器并让其直接子级 `.panel` 回到受限高度内滚动
- `回归情况`：代码已修复，待用户实际滚动确认
- `备注 / 回链`：详见 [requirements/REQ-001-bug-and-stability.md](requirements/REQ-001-bug-and-stability.md)

### `S5-BUG-005`

- `BUG-ID`：`S5-BUG-005`
- `标题`：电力恢复后多余发电量未回充蓄电池
- `发现时间`：2026-03-11
- `发现来源`：Stage5 仿真联调时，由用户反馈
- `当前状态`：`fixed`
- `归属类型`：`cross-requirement`
- `关联需求`：`S5-RQ-001`、`S5-RQ-004`
- `现象 / 影响`：电力不足时仿真会消耗蓄电池，但当发电恢复并出现富余功率后，蓄电池电量不会回升，导致电池只能放电不能充电
- `处理结果`：补齐供电结算中的回充逻辑；在满足当前供电需求后，将当前 tick 的剩余发电功率换算为焦耳并回充进蓄电池，同时受电池容量上限限制
- `回归情况`：代码已修复并通过构建，待用户联调确认
- `备注 / 回链`：详见 [requirements/REQ-001-bug-and-stability.md](requirements/REQ-001-bug-and-stability.md) 与 [requirements/REQ-004-runtime-power-adjustment.md](requirements/REQ-004-runtime-power-adjustment.md)

### bug 条目模板

- `BUG-ID`：`S5-BUG-XXX`
- `标题`：
- `发现时间`：
- `发现来源`：
- `当前状态`：
- `归属类型`：
- `关联需求`：
- `现象 / 影响`：
- `处理结果`：
- `回归情况`：
- `备注 / 回链`：

## 更新日志

- 2026-03-09：建立 Stage5 全局 bug / 中断记录文档。
- 2026-03-09：补录首批 3 个跨需求 bug，均在实现 `S5-RQ-005` 联调过程中发现并已关闭。
- 2026-03-10：新增并修复 `S5-BUG-004`，恢复右侧选中详情面板滚动能力。
- 2026-03-11：新增并修复 `S5-BUG-005`，恢复富余发电量对蓄电池的回充能力。