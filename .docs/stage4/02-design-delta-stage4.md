# Stage4 增量设计（当前迭代）

## 1. 设计目标

- 在不改变领域模型与仿真规则前提下，补齐设备详情功率展示。

## 2. 变更点

- 详情面板新增/补齐 `powerDemand` 展示行。
- 文案补齐：中英文 `detail.powerDemand`（如已有则复用）。
- 发电设备统一显示 `0 kW`。

## 3. 约束

- 不新增与 S4-RQ-001 无关的状态或 UI。
- 不改变既有仿真规则与领域模型结构。

## 4. 兼容性

- 保持既有布局、交互和存档兼容。
