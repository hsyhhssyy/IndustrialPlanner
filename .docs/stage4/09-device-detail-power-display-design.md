# Stage4 专项设计：设备详情功率字段展示

## 1. 目标

- 在设备详情中展示 `powerDemand`，补齐 Stage3 遗漏。

## 2. 数据来源

- 从设备类型定义读取 `powerDemand`。
- 不引入新字段，不做运行时推导替换。

## 3. 展示规则

1. 字段名：功率消耗（或既有命名口径）。
2. 单位：`kW`。
3. 发电设备：显示 `0 kW`。
4. 缺失值兜底：按 `0` 展示并记录排查项。

## 4. 验证点

- 与设备配置值一致。
- 与总耗电聚合口径无冲突。
